import Layout from "@/components/Layout/Layout";
import React from "react";

const Home:React.FC = () => {
    return (
        <Layout>
            {/* More content can go here */}
        </Layout>
    );
};

export default Home;
